// TODO: Root app component
// - Dual-pane layout: RawPane (left) | SteeredPane (right)
// - Central slider (0–100) mapped to steering_multiplier (0.0–2.0)
// - On submit: fire two API calls to Modal /generate (multiplier=0 and multiplier=sliderValue)
// - Pass activation values down to BrainScanner
